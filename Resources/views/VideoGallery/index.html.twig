<h1>VideoGallery list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Playlistid</th>
            <th>Name</th>
            <th>Slug</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('videogallery_admin_edit', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.playlistId }}</td>
            <td>{{ entity.name }}</td>
            <td>{{ entity.slug }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('videogallery_admin_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                    <li>
                        <a href="{{ path('videogallery_admin_synchronize', { 'id': entity.id }) }}">synchronize</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('videogallery_admin_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
