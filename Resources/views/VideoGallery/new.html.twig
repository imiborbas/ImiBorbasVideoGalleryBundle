<h1>VideoGallery creation</h1>

<form action="{{ path('videogallery_admin_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('videogallery_admin') }}">
            Back to the list
        </a>
    </li>
</ul>
